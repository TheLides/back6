@model Backend6.Models.ForumMessageAttachment

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete attachment</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            File
        </dt>
        <dd>
            <div class="row">
                <div class="col-sm-6 col-md-4">
                    <div class="thumbnail">
                        <img src="@Model.FilePath" alt="@Model.FileName" />
                    </div>
                </div>
            </div>
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Created)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Created)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-controller="ForumTopics" asp-action="Details" asp-route-id="@Model.ForumMessage.ForumTopicId">Back to Topic</a>
    </form>
</div>
